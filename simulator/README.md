# 设备模拟器

模拟树莓派设备的数据采集和上报，用于在没有实际硬件时测试系统。

## 功能

- ✅ 模拟温度、压力、湿度传感器数据
- ✅ 模拟YOLOv8人员检测结果
- ✅ 定时上报数据到后端服务器
- ✅ 接收并响应控制指令
- ✅ 模拟生产计数

## 使用方法

### 1. 安装依赖

```bash
cd simulator
pip install aiohttp
```

### 2. 配置服务器地址

编辑 `config.py`：

```python
SERVER_URL = "http://localhost:8000"  # 后端服务器地址
DEVICE_ID = "device_001"              # 设备ID
```

### 3. 启动模拟器

```bash
python main.py
```

## 配置说明

在 `config.py` 中可以调整：

| 配置项 | 说明 | 默认值 |
|--------|------|--------|
| SERVER_URL | 后端服务器地址 | http://localhost:8000 |
| DEVICE_ID | 设备ID | device_001 |
| SENSOR_INTERVAL | 传感器上报间隔(秒) | 3 |
| DETECTION_INTERVAL | 检测上报间隔(秒) | 5 |
| DANGER_ZONE_PROBABILITY | 危险区域入侵概率 | 0.1 (10%) |

## 与真实设备的关系

这个模拟器的代码结构和真实树莓派完全一致：

```
模拟器                          树莓派
├── sensor_simulator.py    →    真实传感器读取
├── detection_simulator.py →    YOLOv8检测
├── device_client.py       →    device_client.py (相同)
└── main.py                →    main.py (相同结构)
```

当你拿到树莓派后，只需要：
1. 将 `sensor_simulator.py` 替换为真实传感器读取代码
2. 将 `detection_simulator.py` 替换为 `zone_detection.py`
3. 修改 `config.py` 中的服务器地址

## 输出示例

```
============================================================
🤖 树莓派设备模拟器
============================================================
📡 服务器地址: http://localhost:8000
🔧 设备ID: device_001
⏱️  传感器上报间隔: 3秒
👁️  检测上报间隔: 5秒
============================================================
按 Ctrl+C 停止模拟器

📊 temperature: 45.2°C
📊 pressure: 103.5kPa
📊 humidity: 52.3%
👁️  检测: 人数=1 ✓
📢 状态变更: stopped -> running
�icing 生产: +2 总计=2
📊 temperature: 65.8°C
👁️  检测: 人数=2 ⚠️ 危险!
```
